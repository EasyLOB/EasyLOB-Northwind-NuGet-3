@model RegionCollectionModel

@{
    string CSHTML = "Region - Index.cshtml";
}

<div id="ZAjax"></div>

<script>
    $(function () {
        try {
            zUrlDictionaryClear();

            $("#ZAjax").load("@(Url.Action("Search", "Region", null, Request.Url.Scheme))", function(responseText, textStatus, jqXHR) { zAjaxLoadComplete(responseText, textStatus, jqXHR); });
        } catch (exception) {
            zAlert(zExceptionMessage("@CSHTML", "function", exception.message));
        }
    });
</script>